# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

Website for a ballet school, built with FastAPI (Python 3.12).

## Status

The repository currently contains only project scaffolding (`venv/`, `.env`, `.gitignore`) — no application code exists yet. There is no `requirements.txt`, no `app/` package, and no entrypoint. Update this file's Commands and Architecture sections as the codebase is built out.

## Environment

- Virtual environment lives in `venv/` — activate with `source venv/bin/activate` before running any Python command.
- Secrets/config belong in `.env` (gitignored).
